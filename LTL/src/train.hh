#ifndef __TRAIN_HH__
#define __TRAIN_HH__

#include "its/ITSModel.hh"


/** Build a type called "trains" representing the n trains problem. */
void loadTrains (int n, its::ITSModel & model, bool isRec = false);

#endif

